//
//  AppointmentConfirmationView.swift
//  FinalProject
//
//  Created by Rauny Martinelli on 2024-02-29.
//

import SwiftUI

struct AppointmentConfirmationView: View {
    var body: some View {
        VStack {
            // Display confirmation details
            Text("Confirm Your Appointment")
            Button("Confirm", action: {
                // Confirm action
            })
            Button("Cancel", action: {
                // Cancel action
            })
            Button("Reschedule", action: {
                // Reschedule action
            })
        }
    }
}
